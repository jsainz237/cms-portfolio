{
  "name": "root",
  "private": true,
  "workspaces": [
    "@portfolio/*"
  ],
  "scripts": {
    "dev": "lerna run dev",
    "build": "lerna run build",
    "deploy": "lerna run deploy"
  },
  "devDependencies": {
    "lerna": "^8.1.9"
  }
}
